import 'package:flutter/material.dart';

class Holdings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('보유 종목')),
        body:Center(child:Text("보유 종목"))
    );
  }
}