import 'package:flutter/material.dart';

class Assets extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('내 자산 ・ 수익률')),
      body:Center(child:Text("내 자산"))
    );
  }
}